{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "deploy": "firebase deploy",
    "deploy-hosting": "firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^9.5.0"
  }
}
